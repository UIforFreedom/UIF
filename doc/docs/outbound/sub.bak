---
sidebar_position: 4
---

# 用 订阅链接 导入

一般购买了代理服务，都会提供 `订阅链接`，目前 UIF 支持 `V2rayN` 、`Clash`、`Clash-Meta` 格式。

## 导入

到 [我的订阅](https://uiforfreedom.github.io/#/out/subscribe)，点击 `添加订阅` 按钮，把订阅链接填入 `订阅地址`，点击右上角 `添加` ，UIF 会自动识别对应格式。

## 更新

更新时需要读取目标网站的内容，因此，更新的方法有两种 `使用代理`，`不使用代理`。

UIF 会自动先用`不使用代理`模式尝试更新，如果失败了则使用`使用代理`模式。
